/target
# Darwin release tarball + minisign (built in CI; served from Vercel — not tracked)
sidekar-darwin-arm64.tar.gz
sidekar-darwin-arm64.tar.gz.minisig
sidekar-extension.zip
.vercel
.DS_Store
screenshots/
native/macos-automation/.build/
# Site download payloads (built in CI / Vercel — not tracked)
www/public/binaries/
.env
.env.*
!.env.example
relay/target/
minisign.key
