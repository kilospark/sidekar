# Relay: Fly.io note

Fly.io is no longer used for the Sidekar relay.

The old `sidekar-relay` Fly app was removed on **March 31, 2026** after the
relay moved to GCP Compute Engine behind `relay.sidekar.dev`.

Use **`context/relay-gcp.md`** for the live deployment procedure.
